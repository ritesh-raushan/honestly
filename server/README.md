# Honestly - Backend

FastAPI backend for **Honestly**, an anonymous feedback web app. Users sign up with email + OTP verification, share a public link (`/u/<username>`), and receive anonymous messages they can read on their dashboard.

## Tech Stack

- **FastAPI** - web framework
- **SQLAlchemy 2.0** - ORM
- **PostgreSQL** - database
- **Pydantic v2 / pydantic-settings** - validation and config
- **passlib + bcrypt** - password hashing
- **python-jose** - JWT (access + refresh tokens)
- **fastapi-mail + Resend** - transactional email

## Project Structure

```
server/
├── app/
│   ├── app.py                  # FastAPI app
│   ├── config.py               # Settings loaded from .env
│   ├── database.py             # SQLAlchemy engine + session
│   ├── models/
│   │   └── model.py            # User and Message ORM models
│   ├── routers/
│   │   ├── auth.py             # /auth/login, /refresh, /logout
│   │   ├── user_management.py  # /auth/register, /verify-email, password reset
│   │   └── feedback.py         # /u/{username}, /messages
│   ├── schemas/                # Pydantic request/response schemas
│   └── utils/
│       ├── auth.py             # JWT dependencies
│       ├── tokens.py           # JWT and OTP helpers
│       └── email_service.py    # HTML email templates
├── docker-compose.yml          # Postgres + Adminer
├── main.py                     # uvicorn entrypoint
└── requirements.txt
```

## Setup

### 1. Prerequisites

- Python 3.11+
- Docker (for Postgres)
- A [Resend](https://resend.com) account for transactional email

### 2. Clone and configure

```bash
git clone <repo>
cd honestly/server
cp .env.example .env
# Edit .env and fill in the secrets
```

### 3. Install dependencies

```bash
python -m venv venv
.venv\Scripts\activate           # Windows
# source .venv/bin/activate      # macOS / Linux
pip install -r requirements.txt
```

### 4. Start the database

```bash
docker compose up -d
```

Postgres runs on `localhost:5432` and Adminer (DB UI) on `http://localhost:8080`.

### 5. Run the server

```bash
python main.py
```

The API is now at `http://localhost:8000` with interactive docs at `http://localhost:8000/docs`.

## Environment Variables

See `.env.example` for the full list. Key groups:

- `ENVIRONMENT` - `development` or `production`. Controls cookie `secure` flag and logging format.
- `DATABASE_*` - Postgres connection
- `SECRET_KEY` / `REFRESH_TOKEN_SECRET_KEY` - separate secrets for access and refresh tokens
- `RESEND_API_KEY` / `MAIL_FROM_ADDRESS` - email sending
- `FRONTEND_URL` / `BACKEND_URL` - used in welcome emails and CORS

On the next app start, `Base.metadata.create_all` will recreate all tables from the current models. Add Alembic before going to production.

## API Docs

Full interactive API documentation is auto-generated by FastAPI:

- Swagger UI - http://localhost:8000/docs
- ReDoc - http://localhost:8000/redoc
